name: ghar_nepal
description: "Ghar Nepal property marketplace Android app"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management: AsyncValue's loading/data/error states mirror the
  # web app's own TanStack-Query-driven empty/loading/error discipline.
  flutter_riverpod: ^2.6.1

  # Declarative routing with redirect-based auth/admin guards, mirroring
  # frontend/src/components/auth/RequireAuth.tsx.
  go_router: ^14.8.1

  # HTTP client with interceptor support (bearer token, 401/422 handling).
  dio: ^5.7.0

  # Secure on-device storage for the Sanctum personal access token.
  flutter_secure_storage: ^11.0.0

  # For the already-absolute media URLs the API returns.
  cached_network_image: ^3.4.1

  # OpenStreetMap tiles via Leaflet-equivalent, matching the web app's
  # map choice (no Google Maps API key needed).
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  # Fetches the same Google Fonts (Plus Jakarta Sans / Inter / Noto Sans
  # Devanagari) the website loads, caching them on-device after first use.
  google_fonts: ^6.2.1

  # Nepali/Indian digit-grouping (lakh/crore) currency formatting.
  intl: ^0.20.2

  # Opens the poster's wa.me WhatsApp link from Listing Detail.
  url_launcher: ^6.3.1

  # Native OS share sheet for the "Share listing" action on Listing Detail.
  share_plus: ^13.3.0

  # Gallery/camera photo picking for the Post-Property Wizard's media step.
  image_picker: ^1.1.2

  # General file picking (jpg/png/pdf) for the Verification Center's KYC
  # document upload — image_picker alone can't select a PDF.
  file_picker: ^12.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Phase 7 branding: generate the launcher icon (incl. Android adaptive
  # icon) and the native splash screen from the source art in assets/icon/
  # — run `dart run flutter_launcher_icons` / `dart run flutter_native_splash:create`
  # after editing the config below, then delete these two dev deps' output
  # is committed (generated Android res files), not the tools themselves.
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.4

# Source art: assets/icon/icon.png (full-bleed trust-green square, used for
# legacy/Play-Store icon contexts) and assets/icon/icon_foreground.png (the
# same white house glyph on a transparent background, sized within
# Android's adaptive-icon safe zone) — both a simple flat house pictogram
# matching the splash screen's existing Icons.home_work_outlined motif.
flutter_launcher_icons:
  android: true
  image_path: "assets/icon/icon.png"
  adaptive_icon_background: "#1F4B3F"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"

flutter_native_splash:
  color: "#1F4B3F"
  image: "assets/icon/splash_logo.png"
  android_12:
    color: "#1F4B3F"
    image: "assets/icon/splash_logo.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/icon/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
